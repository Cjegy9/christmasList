import React from "react";
import Login from "./login";
import CreateList from "../components/createList";

const Home = props => (
  <div>
    <Login />
    <CreateList />
  </div>
);

export default Home;
